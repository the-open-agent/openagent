# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
casibase
openagent

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (CI runs `go mod vendor` only on the runner; never commit vendor/)
vendor/

data/**
!data/template/
!data/template/**
files/
images/

.idea/
*.iml

.claude

tmp/
tmpFiles/
*.tmp
logs/
lastupdate.tmp
commentsRouter*.go

dist/

openagent.db